% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers_parse.R
\name{parse_ais}
\alias{parse_ais}
\title{Parse a raw AIS frame (JSON.parse for AIS)}
\usage{
parse_ais(raw)
}
\arguments{
\item{raw}{(scalar<character>) one raw frame as a JSON string.}
}
\value{
(list) the parsed frame (a nested list).
}
\description{
A thin wrapper over \code{\link[jsonlite:fromJSON]{jsonlite::fromJSON()}} (with \code{simplifyVector = FALSE}, so the
shape is a predictable nested list) — the AIS analogue of JavaScript's
\code{JSON.parse}. Deliberately generic: it does \strong{not} model the 25 message bodies,
because the AISStream API is BETA and its bodies shift. Use it in a handler, or —
more typically — offline over recorded NDJSON. Error frames (\code{{"error": "..."}})
parse like any other object.
}
