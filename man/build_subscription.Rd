% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers_subscription.R
\name{build_subscription}
\alias{build_subscription}
\title{Build the AISStream.io subscription frame (as a JSON string)}
\usage{
build_subscription(
  api_key,
  bounding_boxes,
  message_types = NULL,
  ship_mmsi = NULL
)
}
\arguments{
\item{api_key}{(scalar<character>) the AISStream.io API key (non-empty).}

\item{bounding_boxes}{(list) a non-empty list of boxes (see
\code{\link[=normalise_bounding_boxes]{normalise_bounding_boxes()}}).}

\item{message_types}{(vector<character, 0..> | NULL) optional \code{FilterMessageTypes};
unique values, each one of \link{AIS_MESSAGE_TYPES}. \code{NULL}/empty omits the field.}

\item{ship_mmsi}{(vector<character, 0..> | NULL) optional \code{FiltersShipMMSI}; at most
50 MMSI strings. \code{NULL}/empty omits the field.}
}
\value{
(scalar<character>) the subscription frame as a JSON string.
}
\description{
Assembles the one subscription message AISStream expects within 3 seconds of
connecting: \code{APIKey} and \code{BoundingBoxes} are always present; \code{FilterMessageTypes}
and \code{FiltersShipMMSI} are included \strong{only when non-empty} (the server rejects an
empty \verb{[]}). Bounding boxes are normalised and range-checked, the MMSI filter is
capped at 50, and message types must be a unique subset of \link{AIS_MESSAGE_TYPES}.
\code{auto_unbox = TRUE} keeps the API key and scalar fields as JSON scalars, while
the box/filter arrays stay arrays.
}
