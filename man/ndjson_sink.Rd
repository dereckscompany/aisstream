% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recorder.R
\name{ndjson_sink}
\alias{ndjson_sink}
\title{A durable hourly-NDJSON message sink}
\usage{
ndjson_sink(dir, flush_seconds = 5)
}
\arguments{
\item{dir}{(scalar<character>) output directory; created if missing.}

\item{flush_seconds}{(scalar<numeric in ]0, Inf[>) minimum seconds between flushes
to disk; the data-at-risk window on an abrupt kill. Default \code{5}.}
}
\value{
(function) a handler \verb{function(raw)} suitable for
\code{ais$on("message", ...)}.
}
\description{
The proven "read fast or get dropped" recorder, as a \strong{message-handler factory}.
Returns a \verb{function(raw)} you register yourself — \code{ais$on("message", ndjson_sink(dir))} — exactly the way you would use \code{\link[connectcore:ws_file_sink]{connectcore::ws_file_sink()}},
so the sink is a pure handler with no dependency on the client and reads like Node.
}
\details{
Each frame keeps the durable-write behaviour message-driven (there is no separate
\code{later} timer): on every call it computes the current UTC hour slot and, when the
slot changes (or on the very first frame), closes the previous hourly file and
opens the next one (\verb{<dir>/ais-YYYY-MM-DDTHH.ndjson}) in append mode; it then
appends the \strong{raw} frame plus a newline immediately — the hot path stays
parse-free, one frame per line (NDJSON). Only once at least \code{flush_seconds} have
elapsed since the last flush does it \code{flush()} the connection and reset the timer.

Durability semantics: writes are \strong{append-only}, so any already-flushed line
survives an abrupt kill (\code{SIGKILL}, power loss); the data at risk is bounded to the
frames written in the last \code{flush_seconds} window. The final partial buffer is
flushed when the process exits — the open file connection is flushed and closed on
garbage collection / R shutdown — so a clean stop loses nothing. Parse the
resulting files offline with \code{\link[=parse_ais]{parse_ais()}} and the flatteners.
}
