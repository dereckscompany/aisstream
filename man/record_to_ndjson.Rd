% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recorder.R
\name{record_to_ndjson}
\alias{record_to_ndjson}
\title{Record an AIS stream to durable hourly NDJSON}
\usage{
record_to_ndjson(ais, dir, flush_seconds = 5)
}
\arguments{
\item{ais}{(class<AisStream>) the client to record (must not be open yet).}

\item{dir}{(scalar<character>) output directory; created if missing.}

\item{flush_seconds}{(scalar<numeric in ]0, Inf[>) flush/roll check interval in
seconds. Default \code{5}.}
}
\value{
(class<AisStream>) invisibly, \code{ais} (so the caller can chain \verb{$run()}).
}
\description{
The proven "read fast or get dropped" recorder. It opens the first hourly file
\strong{before} any message arrives, registers a \code{"message"} handler that appends each
\strong{raw} frame (via \code{\link[connectcore:ws_file_sink]{connectcore::ws_file_sink()}} — the minimum work, so the socket
drains fast enough that AISStream never drops you), and schedules a task on the
\code{later} loop that every \code{flush_seconds} flushes the buffer to disk and, on the
turn of a UTC hour, closes the current file and opens the next. One frame per line,
so a crash costs at most the last unflushed window.
}
\details{
This only \strong{wires} the recorder; the caller then drives the loop with \code{ais$run()}
(or \code{ais$connect()} inside a host loop). Parse the resulting files offline with
\code{\link[=parse_ais]{parse_ais()}} and the flatteners.
}
