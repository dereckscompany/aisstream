% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers_subscription.R
\name{normalise_bounding_box}
\alias{normalise_bounding_box}
\title{Normalise a single bounding box to AISStream's nested-corner form}
\usage{
normalise_bounding_box(box)
}
\arguments{
\item{box}{(list) one bounding box, in either supported shape.}
}
\value{
(list) the box as \code{list(c(min_lat, min_lon), c(max_lat, max_lon))}.
}
\description{
Accepts a box in either supported shape and returns AISStream's canonical
\code{list(c(min_lat, min_lon), c(max_lat, max_lon))} (two \verb{[lat, lon]} corners):
\itemize{
\item a \strong{named list} \code{list(min_lat=, min_lon=, max_lat=, max_lon=)} (the JS-object
style), or
\item a \strong{raw nested box} \code{list(c(lat1, lon1), c(lat2, lon2))} (corner order is
irrelevant; the corners are sorted into a min/max pair).
Every latitude is validated to \verb{[-90, 90]} and every longitude to \verb{[-180, 180]}.
}
}
