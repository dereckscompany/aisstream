% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers_parse.R
\name{ais_metadata}
\alias{ais_metadata}
\title{Flatten an AIS frame's MetaData to a one-row data.table}
\usage{
ais_metadata(parsed)
}
\arguments{
\item{parsed}{(list) one frame, already parsed by \code{\link[=parse_ais]{parse_ais()}}.}
}
\value{
(class<data.table>) a one-row data.table of the common metadata.
}
\description{
Pulls the stable common fields from a parsed frame's \code{MetaData} (capital D) into a
single tidy row: \code{message_type}, \code{mmsi}, \code{ship_name} (trimmed), \code{latitude},
\code{longitude}, and \code{time_utc} (POSIXct, via \code{\link[=parse_go_time]{parse_go_time()}}). Present on every
message type, so it is the safe denominator across the feed.
}
